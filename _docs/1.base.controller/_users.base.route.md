<!-- import { Router } from "express";
import BaseUserControllerInstance from "../controllers/_users.base.controller.js";

const router = Router();

router.get("/", (req, res) => res.send("TEST"));

router.get("/users", (req, res) => BaseUserControllerInstance.getAll(req, res));
router.get("/users/:id", (req, res) =>
  BaseUserControllerInstance.getById(req, res)
);

export default router; -->
